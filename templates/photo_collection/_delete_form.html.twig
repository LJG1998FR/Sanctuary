<form method="post" action="{{ path('admin_photo_collection_delete', {'id':photo_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" data-turbo-frame="_top">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo_collection.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash3-fill"></i></button>
</form>
